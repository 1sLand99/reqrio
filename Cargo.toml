[workspace]
resolver = "1"
members = [
    "json",
    "reqtls",
    "reqrio"
]

[workspace.dependencies]
serde = { version = "1.0.228", features = ["derive"] }
reqrio-json = { path = "json", version = "0.1.4" }
reqtls = { path = "reqtls", version = "0.0.11" }

[profile.release]
opt-level = "z"
strip = "symbols"
lto = true
codegen-units = 1
panic = "abort"