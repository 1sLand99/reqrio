[workspace]
resolver = "1"
members = [
    "json",
    "reqtls",
    "reqrio"
]

[workspace.dependencies]
reqtls = { path = "reqtls", version = "0.0.11" }

[profile.release]
opt-level = "z"
strip = "symbols"
lto = true
codegen-units = 1
panic = "abort"